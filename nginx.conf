server {
    listen 80;

    location /api/ {
        proxy_pass http://172.18.0.1:11434/api/;
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding on;
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
}